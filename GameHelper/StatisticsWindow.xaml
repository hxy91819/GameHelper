<Window x:Class="GameHelper.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameHelper"
        mc:Ignorable="d"
        Title="游戏统计" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#f0f0f0">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="#2196F3" CornerRadius="10" Margin="20,20,20,10">
            <TextBlock Text="游戏统计信息" FontSize="24" FontWeight="Bold" Margin="20,15,20,15" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
        </Border>
        
        <TabControl Grid.Row="1" Margin="20,0,20,20" Background="White">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" BorderThickness="1,1,1,0" BorderBrush="Gainsboro" CornerRadius="4,4,0,0" Margin="2,0">
                                    <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center"
                                                      ContentSource="Header" Margin="10,5"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#2196F3" />
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border" Property="Background" Value="LightGray" />
                                        <Setter Property="Foreground" Value="Black"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabItem Header="周统计">
                <DataGrid x:Name="WeeklyStatsGrid" AutoGenerateColumns="False" IsReadOnly="True" Margin="10"
                          Background="White" AlternatingRowBackground="#f9f9f9" GridLinesVisibility="Horizontal" 
                          HorizontalGridLinesBrush="#e0e0e0" RowHeight="30">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="游戏名称" Binding="{Binding GameName}" Width="*"/>
                        <DataGridTextColumn Header="游玩时长" Binding="{Binding PlayTime}" Width="*"/>
                        <DataGridTextColumn Header="游戏次数" Binding="{Binding SessionCount}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="月统计">
                <DataGrid x:Name="MonthlyStatsGrid" AutoGenerateColumns="False" IsReadOnly="True" Margin="10"
                          Background="White" AlternatingRowBackground="#f9f9f9" GridLinesVisibility="Horizontal" 
                          HorizontalGridLinesBrush="#e0e0e0" RowHeight="30">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="游戏名称" Binding="{Binding GameName}" Width="*"/>
                        <DataGridTextColumn Header="游玩时长" Binding="{Binding PlayTime}" Width="*"/>
                        <DataGridTextColumn Header="游戏次数" Binding="{Binding SessionCount}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="年统计">
                <DataGrid x:Name="YearlyStatsGrid" AutoGenerateColumns="False" IsReadOnly="True" Margin="10"
                          Background="White" AlternatingRowBackground="#f9f9f9" GridLinesVisibility="Horizontal" 
                          HorizontalGridLinesBrush="#e0e0e0" RowHeight="30">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="游戏名称" Binding="{Binding GameName}" Width="*"/>
                        <DataGridTextColumn Header="游玩时长" Binding="{Binding PlayTime}" Width="*"/>
                        <DataGridTextColumn Header="游戏次数" Binding="{Binding SessionCount}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,20,20">
            <Button x:Name="RefreshButton" Content="刷新" Width="80" Height="30" Margin="0,0,10,0" Click="RefreshButton_Click"
                    Background="#4CAF50" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="CloseButton" Content="关闭" Width="80" Height="30" Click="CloseButton_Click"
                    Background="#f44336" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
